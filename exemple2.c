main(X, Y) {
                        var tX[X];
                        printf(len(tX));
                        tX[1] = 1;
                        printf(tX[1]);
                        X = tX[Y];
                        printf(X);
                        return(len(tX));
                    }
